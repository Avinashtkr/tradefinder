"""
TradeFinder — Alerts router (re-exported from signals for modularity)
"""
from fastapi import APIRouter
router = APIRouter()
# Alerts endpoints live in signals.py — this file satisfies the main.py import
